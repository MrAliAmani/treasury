[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=backend --cov=frontend --cov-report=term-missing
markers =
    unit: Unit tests
    integration: Integration tests
    visual: Visual/UI tests
    e2e: End-to-end tests 